import {describe,it,expect} from 'vitest'; import {decide} from '../src/policy.js';
describe('policy engine',()=>{it('archives high-confidence noise',()=>expect(decide('noise',.96,'Weekly deals','news@example.com').action).toBe('archived'));it('drafts routine mail',()=>expect(decide('routine',.84,'Project update','client@example.com').action).toBe('drafted_reply'));it('escalates high-stakes content even with confidence',()=>expect(decide('important',.99,'Security alert','security@example.com').action).toBe('escalated'));it('does not act below threshold',()=>expect(decide('noise',.6,'Maybe','x@example.com').action).toBe('none'));});
